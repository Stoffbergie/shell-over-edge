export const sessionTtlMs = 2 * 60 * 60 * 1000;
export const cleanupRetentionMs = 24 * 60 * 60 * 1000;
export const maxResultBytes = 1024 * 1024;
export const maxCommandBytes = 64 * 1024;
export const maxDirectSignalBytes = 32 * 1024;
export const directSignalTtlMs = 60 * 1000;
export const maxDirectSignalsPerRole = 8;
export const defaultTurnCredentialTtlSeconds = sessionTtlMs / 1000;
export const maxTurnCredentialTtlSeconds = 48 * 60 * 60;
